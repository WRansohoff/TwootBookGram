export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "TwootBookGen",
	description: "Demo of how to run an AI service cheaply, with a small language model that can run within serverless functions.",
	navItems: [
	],
	navMenuItems: [
	],
	links: {
	},
};
